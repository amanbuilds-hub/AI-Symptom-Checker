{
  "name": "rural-health-care-root",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Root workspace runner for Rural Health Care",
  "scripts": {
    "start:all": "node start.js",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  }
}
